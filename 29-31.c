/**
	Q29: What is dynamic memory allocation and when it is used?
	A: The method by which memory can be allocated to variables during the run time from the heap space is called dynamic memory allocation.
	
	Q30: What happens if allocated memory is nor freed?
	A: if allocated memory is allocated dynamically and not freed the this is called memory leak. this will result into depelation od memory in heap and make system slowly slowly.

	Q31: What will happen if you try to print a ENUM by using %s?
	A: ENUM are basically number given to specially name. if we try to print enum with %s, %s consider enum as the address of string, and of try to asccess the segv fault will occour.

*/